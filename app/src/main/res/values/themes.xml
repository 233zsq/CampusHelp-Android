<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CampusHelp" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/brand_primary</item>
        <item name="colorPrimaryVariant">@color/brand_primary_dark</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/brand_secondary</item>
        <item name="colorOnSecondary">@color/white</item>
        <item name="android:windowBackground">@color/bg_page</item>
        <item name="android:statusBarColor">@color/brand_primary_dark</item>
        <item name="android:navigationBarColor">@color/bg_card</item>
        <item name="bottomNavigationStyle">@style/Widget.CampusHelp.BottomNavigationView</item>
    </style>

    <style name="Theme.CampusHelp" parent="Base.Theme.CampusHelp" />

    <!-- BottomNavigationView 样式 -->
    <style name="Widget.CampusHelp.BottomNavigationView" parent="Widget.Material3.BottomNavigationView">
        <item name="android:background">@color/bg_card</item>
        <item name="android:elevation">8dp</item>
        <item name="itemIconTint">@color/bottom_nav_color</item>
        <item name="itemTextColor">@color/bottom_nav_color</item>
        <item name="labelVisibilityMode">labeled</item>
    </style>

    <!-- 发布页：任务类型选项 -->
    <style name="PublishTypeChip">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">40dp</item>
        <item name="android:layout_weight">1</item>
        <item name="android:gravity">center</item>
        <item name="android:background">@drawable/bg_publish_type_chip</item>
        <item name="android:textColor">@color/selector_publish_type_text</item>
        <item name="android:textSize">@dimen/text_body</item>
        <item name="android:clickable">true</item>
    </style>

    <!-- 首页：筛选标签 -->
    <style name="FilterChip">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">32dp</item>
        <item name="android:gravity">center</item>
        <item name="android:minWidth">48dp</item>
        <item name="android:paddingHorizontal">10dp</item>
        <item name="android:background">@drawable/bg_publish_type_chip</item>
        <item name="android:textColor">@color/selector_publish_type_text</item>
        <item name="android:textSize">@dimen/text_caption</item>
        <item name="android:clickable">true</item>
    </style>

    <!-- 发布页：文本输入框 -->
    <style name="PublishTextInput" parent="Widget.Material3.TextInputLayout.OutlinedBox">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="boxCornerRadiusBottomEnd">8dp</item>
        <item name="boxCornerRadiusBottomStart">8dp</item>
        <item name="boxCornerRadiusTopEnd">8dp</item>
        <item name="boxCornerRadiusTopStart">8dp</item>
    </style>
</resources>
