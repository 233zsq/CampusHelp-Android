spring:
  datasource:
    url: jdbc:mysql://localhost:3306/campushelp?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: your_password
    driver-class-name: com.mysql.cj.jdbc.Driver

  data:
    redis:
      host: localhost
      port: 6379
      password:
      database: 0
      timeout: 3000ms

# JWT 配置
jwt:
  secret: CampusHelpSecretKeyForJWT2024MustBeLongEnoughForHS256Algorithm
  expiration: 604800000  # 7天 (毫秒)
